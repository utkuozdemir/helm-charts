apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transmission-openvpn.fullname" . }}-test-connection"
  labels:
    {{- include "transmission-openvpn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transmission-openvpn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
